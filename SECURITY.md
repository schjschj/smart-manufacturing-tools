# 내부 테스트 보안 기준

- 공개 GitHub Pages 주소를 직접 공유하지 않고 Edge Auth 주소만 공유한다.
- 업로드 파일은 각 브라우저에서 처리하며 서버로 전송하지 않는다. 연장근무 도구만 명시된 API와 통신한다.
- 사번, 비밀번호, 생산 데이터, 내부 경로를 저장소·브라우저 저장소·콘솔에 기록하지 않는다.
- 공통 암호는 12시간 세션으로 유지하며 로그아웃 또는 암호 교체 시 무효화한다.
- 테스트 종료 후 공통 암호를 폐기하고 사내 SSO와 개인별 권한으로 전환한다.

설비 알람 분석기는 공개 소스가 없어 배포본에서 변경 가능한 안내·접근 제한만 적용 대상이다. 서버 측 파일 격리와 임시파일 삭제는 소스 확보 후 검증한다.
